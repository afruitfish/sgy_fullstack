// ts api/ 不需要jsx 
// interface name password 
import { Component } from 'react'
import { LoginEntity } from '../model/login'
export const isValidLogin = (loginInfo: LoginEntity):Promise<boolean> => new Promise(()=>{
    
})
